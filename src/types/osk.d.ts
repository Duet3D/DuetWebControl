interface Window {
    /**
     * May be set to true to disable code mirror functionality (code highlighting)
     */
    disableCodeMirror: boolean;

    /**
     * Indicates if the on-screen keyboard is currently open
     */
    oskOpen: boolean;
}
